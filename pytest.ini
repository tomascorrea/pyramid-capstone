[tool:pytest]
# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Coverage settings  
addopts = --strict-markers --strict-config --disable-warnings
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')

# Suppress known deprecation warnings from dependencies
# These are not actionable for us and will be fixed by upstream libraries
# Note: Use PYTHONWARNINGS="ignore::DeprecationWarning,ignore::UserWarning" when running tests
